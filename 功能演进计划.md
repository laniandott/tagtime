# TagTime 功能演进计划（产品草案 v0.3）

**文档定位**：这是一个可以随使用反馈调整的功能假设，不是一次性冻结的需求合同。

- 本文档关注产品方向、用户流程和优先级，不规定代码实现细节。
- 阶段表示成熟顺序，不代表固定发布日期。
- 先验证“记录是否自然、数据是否可信”，再扩展洞察和智能建议。
- 新功能必须服从一个底线：不能为了记录打断用户，也不能让历史数据变得不可信。

---

## 一、先统一核心概念

后续功能容易混乱的根源，是把“暂停”“切换活动”“待续”和“续接”当成了一件事。TagTime 先按下面的概念区分：

| 概念 | 含义 |
|------|------|
| 活动记录 | 一段实际发生的计时，例如 10:00—10:35 编码 |
| 中断 | 当前活动被另一项活动接管，重点是“发生了什么切换” |
| 中断栈 | 按后进先出保存被暂停的活动，后开始的活动优先处理 |
| 待续 | 当前活动暂时结束，但用户明确希望以后继续 |
| 续接链 | 同一项活动被分成多段记录，重点是“后来是否接回来了” |
| Todo | 用户计划完成的具体事项或内容，不等于一次计时 |
| 状态打点 | 某一时刻的精力或心情，不是持续占用时间的活动 |

**关键原则**：

1. “续接链”和“中断栈”是两条不同的信息：栈描述注意力如何被接管，续接链描述同一项活动后来如何继续。
2. 暂停一个支持续接的活动时，活动进入待续栈；用户可以立即选择并开始下一项活动，也可以选择稍后处理，不应被迫填写复杂信息。
3. 当前活动结束且栈中仍有活动时，优先处理栈顶活动：继续、结束、开始新活动或稍后处理。
4. “终止本次链路”是高警戒操作，必须填写具体原因；它只结束当前链路的状态，不删除已经发生的时间记录。
5. Todo 与活动通过结构化 `todoId` 关联，备注和点记录用于补充执行上下文，不用标题复制来代替关系。
6. 心情和精力不是同一个维度。第一版只选一个维度，避免采集了很多无法解释的数据。

---

## 二、当前产品基础

TagTime 目前已经有一批可作为后续基础的能力：

- 时间型标签、次数型标签、手动补录和目标统计。
- 计时过程中的点记录、日记、图片/视频附件。
- 日历视图、跨午夜显示和 iCalendar 互通基础。
- 活动内短备注、点记录、日记式事件附注和图片附件，用于保留原始上下文。
- 专注力回收机制的第一版：标签模式、待续记录、同标签续接、“算了”原因和初步碎片化统计。

TagTime 的定位是个人行为、活动和状态的原始事件采集层。它负责忠实记录“什么时候做了什么、被什么打断、当时状态如何以及留下了哪些上下文”，不直接替用户下性格结论。后续由 self-entropy 负责整理、汇总和模式分析，由 POCO 负责提醒、复盘、建议和 AI 交互。

Markdown 笔记、Vault、Wiki 链接、笔记文件监听和笔记图谱不再作为 TagTime 的产品方向。现有代码和历史数据暂时保留，先从主流程和产品入口中退出，避免影响回滚和旧数据读取。

因此，下一步不应继续堆叠入口，而应先观察这套基础机制是否真的顺手，特别是：用户能否理解“待续”、是否愿意填写下一步、是否会积累大量无人处理的待续记录。

---

## 三、需要调整的原始想法

### 1. “暂停关联活动”改成“活动中断栈”

用户预期的主链路是“暂停当前活动，开始另一项活动；后开始的活动结束后，再决定是否回到前一项”。因此，下一版不再把待续仅理解为一个平铺清单，而是增加有顺序的活动中断栈：

- 活动1暂停：活动1进入待续栈，打开活动选择入口并开始活动2；也可以选择稍后处理，不立即开始活动2。
- 活动2暂停：活动2压入栈顶，打开同一个活动选择入口并开始活动3。
- 活动2直接结束：如果活动1仍在栈中，弹出轻量处理入口，提供“继续活动1”“结束活动1”“开始活动3”“稍后处理”。
- 活动3结束时，按同样规则处理当前栈顶，不要求用户一次性处理整个栈。
- “终止本次链路”：高警戒的一键操作，结束当前活动及本次中断栈中的上游待续活动，要求填写具体原因；已经发生的时间、备注和点记录全部保留。
- 支持续接的活动默认使用有序标签；混沌标签默认直接结束，避免把不需要恢复的零散活动塞入中断栈。

“稍后处理”必须保留。它是降低流程负担的安全出口，否则每次活动结束都可能被迫在“继续、结束、开始新活动”之间做决定。

### 2. 自动续接弹窗后置

原计划中的“活动超过 5 分钟且 30 分钟内未开始新活动就弹窗”，容易把提醒本身变成新的打断。第一版只提供待续清单和低频、可关闭的提醒；等有真实使用数据后再决定是否需要自动提示。

### 3. “改标签续接”后置

原计划同时要求续接原标签、改标签续接和算了，但当前续接链的语义是“同一活动的连续片段”。如果允许随意改标签，统计解释会变复杂。建议第一阶段只支持原标签续接，把“标签漂移”作为后续明确设计的能力。

### 4. 心情系统先缩小

“心情/能量、10 级滑块、5 个文字锚点、独立时间线、2×2 模型”一次引入过多。建议先做一个低摩擦的 **1—5 级精力打点**：可选、不阻塞计时、可附在点记录或日记上。使用一段时间后，再决定是否需要单独增加“心情”维度。

### 5. 数据正确性提前

标签合并、跨午夜切片、手动补录、待续记录的刷新/重启恢复等，不应被归到后期的“数据完整性增强”。它们是统计可信度的基础，应在洞察功能之前完成或确认。

### 6. Todo 与活动建立结构化勾稽

Todo 和活动承担不同职责：Todo 是“要完成的具体事项”，Tag 是“活动类型或行为类别”，TimeEntry 是“实际执行的一段时间”，Memo/点记录是“这段执行过程中的观察和补充”。它们不应互相替代。

- 从 Todo 启动活动时，先选择活动标签，再自动带入 Todo；从活动页启动时，也可以搜索并绑定一个 Todo。
- 一个 Todo 可以对应多段 TimeEntry。活动被暂停、续接或在中断栈中回退时，默认继承同一个 `todoId`。
- 当前活动卡片显示已绑定的 Todo，点记录优先关联当前 TimeEntry，再通过 TimeEntry 找到 Todo；Memo 同样可以绑定当前 TimeEntry。这样既支持“备注勾稽”，也支持“点记录勾稽”，但不用在多个地方重复维护 Todo 关系。
- Todo 标题不自动复制成备注。备注只写当时的上下文，例如“先处理客户反馈”，结构化关联仍以 `todoId` 为准。
- 停止计时不等于完成 Todo。Todo 的完成、延期、取消必须由用户明确操作；活动结束只说明这段执行时间结束。
- 如果执行过程中切换到另一个 Todo，默认视为新的上下文并建立新的活动片段，不悄悄改写已经发生的记录归属；允许事后人工修正。

---

## 阶段一：中断与恢复 MVP

目标是让用户在被打断后，能够低成本地停下、记住下一步，并在合适的时候接回来。

### 1.1 活动中断栈与待续闭环

| 序号 | 功能点 | 优先级 | 调整后的说明 |
|------|--------|--------|--------------|
| 1.1.1 | 暂停并压入中断栈 | P0 | 支持续接的活动进入待续栈；保留活动顺序和上游关系 |
| 1.1.2 | 暂停后选择下一活动 | P0 | 打开活动选择入口，可开始新活动，也可选择稍后处理 |
| 1.1.3 | 多层中断 | P0 | 活动2可以暂停并接着开始活动3，按后进先出处理 |
| 1.1.4 | 当前活动结束后的回退入口 | P0 | 有上游待续活动时，提供继续、结束、开始新活动、稍后处理 |
| 1.1.5 | 待续清单 | P0 | 展示整个中断栈，也支持查看暂时脱离栈的待续记录 |
| 1.1.6 | 原标签续接 | P0 | 续接后形成同一条续接链，默认保留原 Todo 关联 |
| 1.1.7 | 修改续接提示 | P1 | 续接前允许修正“回来后先做什么” |
| 1.1.8 | 算了并填写原因 | P0 | 结束单个待续意图但保留历史，统计默认排除 |
| 1.1.9 | 终止本次链路 | P0 | 高警戒操作；结束当前链路中的活动和待续项，具体原因必填 |
| 1.1.10 | 轻量提醒 | P2 | 默认关闭或低频提醒；不能强迫用户立即处理 |

**第一阶段暂不做**：自动判断 5 分钟/30 分钟规则、系统级“杂事”标签、自动替用户判断 Todo 是否完成。

### 1.2 Todo 与活动关联

| 序号 | 功能点 | 优先级 | 说明 |
|------|--------|--------|------|
| 1.2.1 | 从 Todo 启动活动 | P0 | 选择 Todo 后进入活动选择或直接启动默认活动 |
| 1.2.2 | 从活动绑定 Todo | P0 | 自由活动开始时可搜索并绑定 Todo，也允许不绑定 |
| 1.2.3 | 中断栈继承 Todo | P0 | 同一 Todo 的暂停、回退和续接默认继承 `todoId` |
| 1.2.4 | 活动卡片显示 Todo | P1 | 当前计时和历史记录都能看出实际执行的具体事项 |
| 1.2.5 | Memo/点记录勾稽 | P1 | 记录优先关联 TimeEntry，再间接关联 Todo，减少重复字段 |
| 1.2.6 | Todo 状态独立维护 | P0 | 活动停止不自动完成 Todo，完成由用户明确操作 |
| 1.2.7 | 事后修正关联 | P1 | 手动补录或复盘时允许补绑、改绑 Todo，并保留修改痕迹 |

### 1.3 最小上下文

| 序号 | 功能点 | 优先级 | 说明 |
|------|--------|--------|------|
| 1.3.1 | 续接链标识 | P1 | 列表显示“几段”，详情可查看完整链条 |
| 1.3.2 | 中断栈状态 | P0 | 明确当前活动、栈顶待续活动和更早的上游活动 |
| 1.3.3 | 待续卡片显示下一步提示 | P0 | 用户打开清单即可回忆，不必再次进入详情 |
| 1.3.4 | 可选的中断活动 | P2 | 只有用户愿意时，才记录“我是被什么打断的” |
| 1.3.5 | 计时详情链路时间线 | P1 | 展示开始、暂停、接管、回退、续接和放弃的顺序 |

### 1.4 精力打点试验版

| 序号 | 功能点 | 优先级 | 说明 |
|------|--------|--------|------|
| 1.4.1 | 1—5 级精力值 | P1 | 可选输入，默认不影响开始/停止计时 |
| 1.4.2 | 计时卡片快捷入口 | P1 | 在当前计时卡片内完成，不跳转页面 |
| 1.4.3 | 点记录/日记附加精力值 | P2 | 用于积累可解释的数据 |
| 1.4.4 | 精力功能开关 | P2 | 用户可以完全关闭，不影响时间记录 |

---

## 阶段二：数据可信与基础洞察

这一阶段先解决“统计能不能相信”，再把数据变成用户看得懂的反馈。

### 2.1 数据基础

| 序号 | 功能点 | 优先级 | 说明 |
|------|--------|--------|------|
| 2.1.1 | 跨午夜切片统计 | P0 | 每日时长只计算当天实际交集 |
| 2.1.2 | 批量补录和批量修正 | P1 | 降低历史数据整理成本 |
| 2.1.3 | 标签归档/合并 | P1 | 保留历史查询和统计，不直接删除旧语义 |
| 2.1.4 | 待续记录恢复规则 | P0 | 浏览器刷新、服务重启、重复续接都不能丢失或重复计算 |
| 2.1.5 | Todo 关联完整性 | P0 | 中断、续接、手动补录和修正都不能丢失或错误继承 Todo |
| 2.1.6 | 导出与删除 | P1 | 用户能导出自己的时间、Todo、状态和上下文关联数据，也能删除状态数据 |

### 2.2 上下文切换洞察

| 序号 | 功能点 | 优先级 | 说明 |
|------|--------|--------|------|
| 2.2.1 | 碎片化指数 | P1 | 先展示计算结果，再允许展开原始记录 |
| 2.2.2 | 上下文切换税 | P1 | 切换次数、平均中断间隔、最长恢复等待；避免把估算说成事实 |
| 2.2.3 | 打断来源分布 | P2 | 需要中断关系数据积累后再做 |
| 2.2.4 | 恢复成本 | P2 | 定义为中断后重新开始原活动所需时间，并明确样本不足提示 |
| 2.2.5 | Todo 执行碎片化 | P1 | 展示一个 Todo 被拆成多少段、被中断多少次，不直接评价用户 |

### 2.3 时间回顾

| 序号 | 功能点 | 优先级 | 说明 |
|------|--------|--------|------|
| 2.3.1 | 今日时间故事 | P1 | 按时间顺序回放活动、Todo、待续、日记和精力打点 |
| 2.3.2 | 周度回顾 | P1 | 先做可解释的事实汇总，不急着生成长篇文字 |
| 2.3.3 | “算了”原因聚合 | P2 | 统计原因类别和次数，词云不是必需品 |
| 2.3.4 | 高光/低谷时段 | P2 | 必须说明依据，不能只给一个没有上下文的评分 |

### 2.4 精力洞察

| 序号 | 功能点 | 优先级 | 说明 |
|------|--------|--------|------|
| 2.4.1 | 精力趋势 | P2 | 周/月趋势，样本不足时明确提示 |
| 2.4.2 | 精力与活动对比 | P2 | 只描述相关性，不声称因果关系 |
| 2.4.3 | 心情独立时间线 | P3 | 等用户确认精力和心情确实需要分开采集后再做 |

---

## 阶段三：反思与决策支持

目标是从“告诉用户发生了什么”逐步走向“帮助用户做下一次选择”。所有建议都必须能回溯到数据依据。

### 3.1 专注窗口与活动建议

| 序号 | 功能点 | 优先级 | 说明 |
|------|--------|--------|------|
| 3.1.1 | 最佳专注窗口 | P1 | 基于历史中断率、持续时长和精力值识别 |
| 3.1.2 | 活动—状态匹配 | P2 | 用户选择活动时提供温和提示，可关闭 |
| 3.1.3 | 最佳一天复盘 | P2 | 先生成可编辑的复盘模板，再考虑自动生成文字 |
| 3.1.4 | 反事实建议 | P3 | 只能作为假设表达，例如“可能减少切换”，不能伪装成确定收益 |

### 3.2 标签漂移与续接扩展

| 序号 | 功能点 | 优先级 | 说明 |
|------|--------|--------|------|
| 3.2.1 | 改标签续接 | P1 | 明确它仍属于同一任务，还是已经成为新活动 |
| 3.2.2 | 标签合并历史保留 | P1 | 搜索、统计和活动上下文关联不丢失 |
| 3.2.3 | 中断图谱 | P2 | 展示活动之间的切换关系 |
| 3.2.4 | Todo 执行事实汇总 | P1 | 汇总实际投入时长、执行段数、中断和完成状态，不直接替用户评价产出 |

### 3.3 高级状态模型

| 序号 | 功能点 | 优先级 | 说明 |
|------|--------|--------|------|
| 3.3.1 | 心情与精力拆分 | P2 | 两个维度分别定义，避免混用 |
| 3.3.2 | 情境维度 | P3 | 地点、人物等信息必须低摩擦且可选 |
| 3.3.3 | 2×2 状态矩阵 | P3 | 数据量足够、用户明确需要时再引入 |

---

## 阶段四：生态互通与长期能力

后续重点是让原始事件数据可迁移、可扩展，并供 self-entropy、POCO 等项目消费，而不是继续在 TagTime 内增加知识库入口。

| 序号 | 功能点 | 优先级 | 说明 |
|------|--------|--------|------|
| 4.1 | 完善 ICS 导出/导入 | P1 | 明确重复事件、时区和删除同步规则 |
| 4.2 | Toggl / Traggo 导入 | P2 | 提供老用户迁移路径，并保留原始字段 |
| 4.3 | 开放 REST API + Token | P2 | 首先定义权限、撤销和本地服务访问边界 |
| 4.4 | 自动归类建议 | P2 | 建议必须可接受、拒绝和纠正；不应改变原始记录 |
| 4.5 | 下游分析接口 | P2 | 为 self-entropy、POCO 提供稳定导出/API，明确字段、权限和版本 |
| 4.6 | AI 复盘助手 | P3 | 不在 TagTime 内优先实现；由 POCO 等下游项目承接，先解决隐私、数据范围、删除和是否联网 |

---

## 五、阶段验收门槛

### 阶段一通过条件

- 用户可以清楚区分“直接结束”和“暂存待续”。
- 待续记录支持多条并存，刷新或重启后仍然存在。
- 同一条待续记录不能被重复续接。
- 活动1暂停后可以开始活动2，活动2暂停后可以开始活动3；活动结束时能按栈顶顺序回退处理。
- 存在上游待续活动时，用户可以继续、结束、开始新活动或稍后处理，不会被迫一次性清空整条链路。
- “终止本次链路”需要具体原因，并且只改变链路状态，不删除历史时间和上下文。
- “算了”不会删除历史，也不会继续污染默认统计。
- Todo 关联在暂停、续接、手动补录和点记录/备注勾稽中保持一致。
- 次数型标签、手动补录、日历、短备注、点记录和附件功能不被新的待续流程影响。
- 使用过程中不会频繁弹出新的强制确认。

### 阶段二通过条件

- 每个指标都能展开到原始时间记录和关联 Todo。
- 跨午夜、未结束、待续和已放弃记录的统计规则清楚。
- 样本不足时显示“数据不足”，不生成貌似精确的结论。
- 用户可以导出并删除状态类数据。

### 阶段三通过条件

- 建议能够说明依据和不确定性。
- 用户可以关闭建议，不影响基础记录功能。
- TagTime 不直接输出未经依据支持的性格结论；下游分析必须能回溯到原始事件。
- 下游项目不能默认读取用户未授权的上下文内容。

---

## 六、当前建议优先级

1. **P0：稳定并验证现有待续机制**：有序/混沌规则、待续清单、同标签续接、“算了”、统计排除。
2. **P0：设计并验证活动中断栈**：暂停接管、多层中断、栈顶回退、稍后处理和终止链路。
3. **P0/P1：补齐 Todo 勾稽**：从 Todo 启动、从活动绑定、链路继承、点记录/备注关联和事后修正。
4. **P1：补齐体验闭环**：续接提示可编辑、续接链详情、轻量提醒开关、最小精力打点。
5. **P1：先做可解释的基础洞察**：碎片化、切换次数、Todo 执行分段、今日时间故事和周度事实汇总。
6. **P2/P3：再做复杂状态模型和下游智能能力**：等数据量、使用习惯和隐私边界都经过验证。

---

**下一次评审问题**：连续使用一段时间后，用户最常遇到的是“忘了下一步”“不想填写”“待续太多”“中断栈回退太频繁”，还是“Todo 关联不自然”？答案会决定下一轮功能往哪边长。

*修订时间：2026-09-04*
*计划性质：可调整的产品假设与优先级草案*
